<?xml version="1.0" encoding="utf-8"?>
<!--
  English only, deliberately. PRODUCT.md's v1 exclusion list names localisation
  explicitly: no Turkish, no other language. The rest of the UI copy lives inline in the
  Compose screens, exactly as it does in the SwiftUI views, so the two codebases can be
  diffed line for line when the wording changes.
-->
<resources>
    <!--
      What the launcher shows. The product is Nagg; "Lockin" is an internal name that
      survives in the package id, the theme names and the preferences file, exactly as it
      survives in the iOS bundle id. Users never see those. They do see this one, and it
      was still the old name.
    -->
    <string name="app_name">Nagg</string>
</resources>
